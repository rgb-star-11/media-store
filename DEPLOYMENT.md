# راهنمای استقرار Media Store

## پیش‌نیازها

- Node.js 20 یا جدیدتر و MongoDB Atlas (یا MongoDB روی سرور)
- یک هاست Node.js؛ هاست اشتراکی PHP به‌تنهایی برای اجرای این API مناسب نیست.
- HTTPS فعال روی دامنه

## پیکربندی امن

1. در پوشه `server`، فایل `.env` را از روی `.env.example` بسازید. این فایل را هرگز در Git، cPanel File Manager عمومی، یا کنار فایل‌های قابل دانلود قرار ندهید.
2. برای `JWT_SECRET` یک مقدار تصادفی بلند قرار دهید، `CLIENT_ORIGIN` را دقیقاً روی دامنهٔ HTTPS سایت بگذارید، و اطلاعات MongoDB/SMS را کامل کنید.
3. مقادیر `INITIAL_ADMIN_*` فقط برای اجرای اول لازم‌اند. پس از ساخته‌شدن مدیر، آن‌ها را از متغیرهای محیطی حذف کنید.
4. به‌علت وجود اسرار قبلی در پروژه، رمز مدیر، کلید JWT و کلید سرویس پیامک را در پنل‌های مربوطه فوراً تعویض کنید.

## ساخت و اجرا

از ریشهٔ پروژه اجرا کنید:

```bash
cd client && npm ci && npm run build
cd ../server && npm ci && npm start
```

سرور فایل‌های `client/dist` و API را از یک دامنه سرو می‌کند. برای اجرای پایدار، آن را با Passenger در cPanel یا PM2 پشت Nginx اجرا کنید. مسیر آغازین برنامه `server/server.js` است و متغیر `PORT` را هاست تعیین می‌کند.

## cPanel / Passenger

1. در **Setup Node.js App**، نسخه Node 20+ و Application root را پوشه `server` انتخاب کنید و Application startup file را `server.js` قرار دهید.
2. پوشهٔ `client` باید کنار `server` باقی بماند؛ ابتدا build را اجرا کنید تا `client/dist` ایجاد شود.
3. در بخش Environment Variables، همهٔ متغیرهای `.env` را وارد کنید (به‌جز متغیرهای اولیه پس از ساخت مدیر).
4. `npm install` را در هر دو پوشه اجرا کنید و سپس برنامه را Restart کنید.
5. برای فایل‌های رسانه‌ای، از `server/uploads` پشتیبان منظم بگیرید. روی هاست‌های بدون دیسک پایدار، این پوشه را به object storage منتقل کنید.

## بررسی پس از استقرار

- `https://دامنه/wp-json/studio/v1/settings` باید JSON برگرداند.
- در مرورگر، یک اثر و سپس یک پیام آزمایشی ایجاد کنید.
- با حساب مدیر وارد شوید و آپلود، ویرایش و حذف را آزمایش کنید.
- بدون ورود، درخواست‌های مسیرهای مدیر باید پاسخ 401/403 بدهند؛ فایل اصلی نیز نباید URL عمومی داشته باشد.

## پرداخت

در این نسخه checkout فقط یک شبیه‌ساز داخلی است و اتصال به درگاه واقعی ندارد. پیش از فروش واقعی باید callback امضاشدهٔ درگاه (مثلاً زرین‌پال) پیاده‌سازی و وضعیت سفارش فقط پس از تأیید callback به `completed` تغییر کند.
